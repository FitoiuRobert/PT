#include <stdio.h>
#include <time.h>
#include <stdlib.h>

#DEFINE N 10



int main(void)
{
  int num[N];
  srand(time(NULL));

  for(int i = 0; i < N; ++i)
    {
      num[i] = rand()%100;
      printf("%d ",num[i]);
    }
  puts();
  
  return 0;
}
